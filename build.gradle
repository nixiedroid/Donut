plugins {
    id("java")
}

group = "com.nixiedroid.Donut"
version = "1.0"

repositories {
    mavenCentral()
}
jar {
    manifest {
        attributes(
                'Main-Class': 'com.nixiedroid.Donut.Main'
        )
    }



}
dependencies {
    testImplementation(platform("org.junit:junit-bom:5.9.1"))
    testImplementation("org.junit.jupiter:junit-jupiter")
}
test {
  //  jvmArgs '--enable-preview'
    useJUnitPlatform()
}